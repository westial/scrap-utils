#!/usr/bin/expect -f
# telnet into tor and reload torrc configuration file

set port [lindex $argv 0]

spawn telnet 127.0.0.1 $port
expect "Escape character is '^]'."
send "AUTHENTICATE\r"
expect "250 OK"
send "signal HUP\r"
expect "250 OK"
send "quit\r"
